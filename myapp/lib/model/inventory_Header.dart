class InventoryH {
  String no;
  String type;
  String locationCd;
  DateTime creation;
  DateTime release;
  DateTime close;
  String status;

  InventoryH(this.no, this.type, this.locationCd, this.creation, this.release,
      this.close, this.status);
}
